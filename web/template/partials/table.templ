package partials

templ Table(id string) {
	<div id={ id } class="flex flex-col gap-2 flex-1">
		<div class="relative w-full overflow-auto">
			<table class="w-full caption-bottom text-sm">
				{ children... }
			</table>
		</div>
	</div>
}
